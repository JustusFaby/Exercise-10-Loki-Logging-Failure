apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-config
  namespace: logging
data:
  config.alloy: |
    loki.write "default" {
      endpoint {
        url = "http://loki:3100/loki/api/v1/push"

        basic_auth {
          username = "wronguser"
          password = "wrongpassword"
        }
      }
    }